/** Spacing scale from the Stitch mockups' tailwind.config. `base` and `xs` are both 4px
 * (the mockups used both names) — prefer `xs` in new code. */
export const Spacing = {
  xs: 4,
  sm: 8,
  base: 4,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
  xxxl: 64,
  gutter: 24,
  marginMobile: 16,
} as const;
